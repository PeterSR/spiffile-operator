## What & why

<!-- What does this change, and what problem does it solve? Link the issue if one exists. -->

## How was it tested?

<!-- go test, helm template, a kind cluster, ... -->

## Checklist

- [ ] Tests added/updated for behavior changes
- [ ] `gofmt`, `go vet` and `go test ./...` pass locally
- [ ] `deploy/crd.yaml` and `charts/spiffile-operator/crds/crds.yaml` kept identical (if CRDs changed)
- [ ] Docs updated (README / docs/) where behavior is user-visible
