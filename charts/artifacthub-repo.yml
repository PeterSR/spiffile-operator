# Artifact Hub repository metadata. For OCI-hosted charts this file is not
# read from git — push it to the registry alongside the chart so Artifact Hub
# can verify ownership ("verified publisher"):
#
#   oras push ghcr.io/petersr/charts/spiffile-operator:artifacthub.io \
#     --config /dev/null:application/vnd.cncf.artifacthub.config.v1+yaml \
#     charts/artifacthub-repo.yml:application/vnd.cncf.artifacthub.repository-metadata.layer.v1.yaml
#
# Set repositoryID after creating the repository entry on artifacthub.io
# (Control Panel -> Add repository -> kind "Helm charts OCI",
#  URL oci://ghcr.io/petersr/charts/spiffile-operator).
repositoryID: dfa51c13-f594-499e-881f-023204c8f032
owners:
  - name: Peter Severin Rasmussen
